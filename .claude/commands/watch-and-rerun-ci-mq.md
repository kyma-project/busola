Watch a busola PR's merge queue CI and re-add to the queue on flaky failures until the PR is merged.

Usage: `/watch-and-rerun-ci-mq [PR Number]`

Read `.agents/skills/watch-and-rerun-ci-mq/SKILL.md` and follow its instructions exactly.

The PR to watch: $ARGUMENTS
