Watch a busola release workflow run's CI and rerun only the failed (flaky) jobs until the release run is green.

Usage: `/watch-and-rerun-ci-release [run URL or run ID]`

Read `.agents/skills/watch-and-rerun-ci-release/SKILL.md` and follow its instructions exactly.

The release run to watch: $ARGUMENTS
