export const bindingVariables = {
  setEnvPrefix:
    'Defines the prefix for environment variables that the ServiceBindingUsage injects. Prefixing is disabled by default.',
  serviceBinding:
    'To use a ServiceInstance, you need credentials for the given service. One ServiceInstance can have numerous credentials to use in a Deployment or Function. When you raise a binding request, the system creates a Secret with credentials in a given Environment. Fill in the form to obtain the credentials.',
  serviceBingingUsage:
    'A custom resource that allows the ServiceBindingUsage controller to inject Secrets into a given Deployment or Function.',
};
