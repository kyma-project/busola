{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "Schema defining structure of list configuration",
  "required": [
    "columns"
  ],
  "title": "Custom pages to be displayed in Busola",
  "type": "object",
  "properties": {
    "columns": {
      "description": "Configuration of fields/data that should be displayed on the table of resource list page.",
      "$ref": "#/definitions/component_array_or_object"
    }
  },
  "definitions": {
    "component_config": {
      "type": "object",
      "properties": {
        "name": {
          "description": "Name of component used for dispaying translations",
          "type": "string"
        },
        "language": {
          "description": "parameter passed to a widget CodeViewer defining language in which editor should parse the code",
          "enum": [
            "json",
            "yaml"
          ],
          "type": "string"
        },
        "path": {
          "description": "The path to the resource variable",
          "examples": [
            "status.phase",
            "metadata.labels"
          ],
          "type": "string"
        },
        "widget": {
          "description": "Name of a widget that should be used for displaying data defined by path",
          "examples": [
            "Badge",
            "CodeViewer"
          ],
          "type": "string"
        }
      }
    },
    "component_array_or_object": {
      "anyOf": [
        {
          "type": "object",
          "oneOf": [
            {
              "required": [
                "path"
              ],
              "not": {
                "required": [
                  "name"
                ]
              }
            },
            {
              "required": [
                "name"
              ],
              "not": {
                "required": [
                  "path"
                ]
              }
            },
            {
              "required": [
                "name",
                "path"
              ]
            }
          ],
          "anyOf": [
            {
              "$ref": "#/definitions/component_config"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "type": "object",
            "oneOf": [
              {
                "required": [
                  "path"
                ],
                "not": {
                  "required": [
                    "name"
                  ]
                }
              },
              {
                "required": [
                  "name"
                ],
                "not": {
                  "required": [
                    "path"
                  ]
                }
              },
              {
                "required": [
                  "name",
                  "path"
                ]
              }
            ],
            "anyOf": [
              {
                "$ref": "#/definitions/component_config"
              }
            ]
          }
        }
      ]
    }
  }
}