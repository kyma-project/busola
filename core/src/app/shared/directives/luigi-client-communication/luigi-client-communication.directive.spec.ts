import { LuigiClientCommunicationDirective } from './luigi-client-communication.directive';

describe('LuigiClientCommunicationDirective', () => {
  it('should create an instance', () => {
    const directive = new LuigiClientCommunicationDirective();
    expect(directive).toBeTruthy();
  });
});
