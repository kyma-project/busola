import { ModalConfig } from 'fundamental-ngx/public_api';

export const EMPTY_TEXT = '-';

export const DEFAULT_MODAL_CONFIG: ModalConfig = {
  maxWidth: '95%',
  minWidth: '5em',
  maxHeight: '95%',
  minHeight: '5em'
};
