apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  labels:
    app: busola
spec:
  replicas: 1
  selector:
    matchLabels:
      app: busola
  template:
    metadata:
      labels:
        app: busola
    spec:
      containers:
        - name: busola
          image: ddagaa/busola-core:1
          imagePullPolicy: Always
          resources:
            limits:
              cpu: 100m
              memory: 30Mi
            requests:
              cpu: 1m
              memory: 6m
          livenessProbe:
            httpGet:
              path: /healthz
              port: 6080
            initialDelaySeconds: 10
          ports:
            - containerPort: 6080
            - containerPort: 8080
        - name: core-ui
          image: ddagaa/busola-core-ui:1
          imagePullPolicy: Always
          resources:
            limits:
              cpu: 100m
              memory: 30Mi
            requests:
              cpu: 1m
              memory: 6m
          livenessProbe:
            httpGet:
              path: /healthz
              port: 6081
            initialDelaySeconds: 10
          ports:
            - containerPort: 6081
            - containerPort: 80
