{
  "name": "ui-tests",
  "description": "Smoke tests for Busola",
  "scripts": {
    "test:cluster": "CYPRESS_LOCAL_DEV=true  $(npm bin)/cypress run --browser chrome --headless",
    "start": "$(npm bin)/cypress open",
    "start:local": "CYPRESS_LOCAL_DEV=true $(npm bin)/cypress open"
  },
  "devDependencies": {
    "cypress-file-upload": "^5.0.7"
  },
  "optionalDependencies": {
    "cypress": "^7.3.0"
  }
}
