{
  "name": "ui-tests",
  "description": "Smoke tests for Busola change literally nothing",
  "scripts": {
    "test:cluster": "$(npm bin)/cypress run --browser chrome --headless",
    "start": "$(npm bin)/cypress open"
  },
  "devDependencies": {
    "cypress-file-upload": "^5.0.7"
  },
  "optionalDependencies": {
    "cypress": "^7.3.0"
  }
}
