{
  "name": "ui-tests",
  "description": "Smoke tests for Busola",
  "scripts": {
    "test:cluster": "$(npm bin)/cypress run --browser chrome --headless",
    "start": "$(npm bin)/cypress open"
  },
  "devDependencies": {
    "cypress": "^7.3.0",
    "cypress-file-upload": "^5.0.7"
  }
}
