{
  "name": "ui-tests",
  "description": "Smoke tests for Busola",
  "scripts": {
    "start": "$(npm bin)/cypress open",
    "start:local": "CYPRESS_LOCAL_DEV=true $(npm bin)/cypress open",
    "test": "$(npm bin)/cypress run --browser chrome --headless",
    "test:local": "CYPRESS_LOCAL_DEV=true $(npm bin)/cypress run --browser chrome --headless"
  },
  "devDependencies": {
    "cypress-file-upload": "^5.0.7"
  },
  "optionalDependencies": {
    "cypress": "^7.4.0"
  }
}
